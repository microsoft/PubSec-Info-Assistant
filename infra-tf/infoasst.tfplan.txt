
Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  ~ update in-place

Terraform will perform the following actions:

  # module.backend.azurerm_linux_web_app.app_service will be updated in-place
  ~ resource "azurerm_linux_web_app" "app_service" {
      ~ app_settings                                   = {
          ~ "AZURE_CLIENT_SECRET"                   = (sensitive value)
            # (38 unchanged elements hidden)
        }
      ~ ftp_publish_basic_authentication_enabled       = false -> true
        id                                             = "/subscriptions/0d4b9684-ad97-4326-8ed0-df8c5b780d35/resourceGroups/infoasst-ryon-tf-1a/providers/Microsoft.Web/sites/infoasst-web-wlsgp"
        name                                           = "infoasst-web-wlsgp"
        tags                                           = {}
      ~ webdeploy_publish_basic_authentication_enabled = false -> true
        # (18 unchanged attributes hidden)

      ~ logs {
            # (2 unchanged attributes hidden)

          ~ http_logs {
              ~ file_system {
                  ~ retention_in_days = 0 -> 1
                    # (1 unchanged attribute hidden)
                }
            }

            # (1 unchanged block hidden)
        }

        # (3 unchanged blocks hidden)
    }

  # module.backend.azurerm_monitor_diagnostic_setting.diagnostic_logs will be updated in-place
  ~ resource "azurerm_monitor_diagnostic_setting" "diagnostic_logs" {
        id                         = "/subscriptions/0d4b9684-ad97-4326-8ed0-df8c5b780d35/resourceGroups/infoasst-ryon-tf-1a/providers/Microsoft.Web/sites/infoasst-web-wlsgp|infoasst-web-wlsgp"
        name                       = "infoasst-web-wlsgp"
        # (2 unchanged attributes hidden)

      - log {
          - category = "AppServiceAppLogs" -> null
          - enabled  = true -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = true -> null
            }
        }
      - log {
          - category = "AppServiceAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceConsoleLogs" -> null
          - enabled  = true -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = true -> null
            }
        }
      - log {
          - category = "AppServiceHTTPLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceIPSecAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServicePlatformLogs" -> null
          - enabled  = true -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = true -> null
            }
        }
      + log {
          + category = "AppServiceAppLogs"
          + enabled  = true

          + retention_policy {
              + days    = 0
              + enabled = true
            }
        }
      + log {
          + category = "AppServiceConsoleLogs"
          + enabled  = true

          + retention_policy {
              + days    = 0
              + enabled = true
            }
        }
      + log {
          + category = "AppServicePlatformLogs"
          + enabled  = true

          + retention_policy {
              + days    = 0
              + enabled = true
            }
        }

        # (4 unchanged blocks hidden)
    }

  # module.enrichmentApp.azurerm_monitor_diagnostic_setting.example will be updated in-place
  ~ resource "azurerm_monitor_diagnostic_setting" "example" {
        id                         = "/subscriptions/0d4b9684-ad97-4326-8ed0-df8c5b780d35/resourceGroups/infoasst-ryon-tf-1a/providers/Microsoft.Web/sites/infoasst-enrichmentweb-wlsgp|example"
        name                       = "example"
        # (2 unchanged attributes hidden)

      - log {
          - category = "AppServiceAntivirusScanAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceAppLogs" -> null
          - enabled  = true -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = true -> null
            }
        }
      - log {
          - category = "AppServiceAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceConsoleLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceFileAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceHTTPLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServiceIPSecAuditLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      - log {
          - category = "AppServicePlatformLogs" -> null
          - enabled  = false -> null

          - retention_policy {
              - days    = 0 -> null
              - enabled = false -> null
            }
        }
      + log {
          + category = "AppServiceAppLogs"
          + enabled  = true

          + retention_policy {
              + days    = 0
              + enabled = true
            }
        }

        # (2 unchanged blocks hidden)
    }

  # module.functions.azurerm_function_app.function_app will be updated in-place
  ~ resource "azurerm_function_app" "function_app" {
      ~ app_settings                    = {
          ~ "AzureWebJobsStorage"                        = (sensitive value)
          ~ "WEBSITE_CONTENTAZUREFILECONNECTIONSTRING"   = (sensitive value)
            # (50 unchanged elements hidden)
        }
        id                              = "/subscriptions/0d4b9684-ad97-4326-8ed0-df8c5b780d35/resourceGroups/infoasst-ryon-tf-1a/providers/Microsoft.Web/sites/infoasst-func-wlsgp"
        name                            = "infoasst-func-wlsgp"
        tags                            = {}
        # (18 unchanged attributes hidden)

        # (4 unchanged blocks hidden)
    }

  # module.kvModule.azurerm_key_vault_secret.spClientKeySecret will be updated in-place
  ~ resource "azurerm_key_vault_secret" "spClientKeySecret" {
        id                      = "https://infoasst-kv-wlsgp.vault.azure.net/secrets/AZURE-CLIENT-SECRET/188080cf841c4d709c7b420e7c43748c"
        name                    = "AZURE-CLIENT-SECRET"
        tags                    = {}
      ~ value                   = (sensitive value)
        # (5 unchanged attributes hidden)
    }

Plan: 0 to add, 5 to change, 0 to destroy.
